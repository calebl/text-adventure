# Sequential confirmation protocol, stored before live calls

Use the unchanged first eight turns of `lib/eval/scripts/the-salt-assizes.yml`, four independent repetitions of the frozen original source followed by four of the candidate. The Salt Assizes is the existing held-out world. This prefix reaches examination, four successive NPC conversations, a dropped item, first entry into a generated room and the return arrival. It does not measure the full twenty-turn script, the tuning worlds, long-term NPC realism, or every newly supported NPC action.

Run the existing `script/eval_run.rb` unchanged. Keep its classifier, model-generated world details and interaction passes: branch divergence, failed calls and unintended consequences are observations, never silently retried. Score retained databases using the unchanged `Eval::RunSet` and compare using `Eval::Comparison` / `Eval::Noise` with four repetitions per side. Keep richness beside defect rates and inspect the actual prose. Report unjudgeable checks and missing branches explicitly; do not pool these runs with earlier full-script baselines.

Each repetition starts from a SQLite backup of the same pre-existing seeded evaluation base. Candidate copies receive the required schema migrations before any calls; the source base and development databases remain untouched. Before source: frozen `8dd1f5c` at `/tmp/text-adventure-r01-r05-before-20260908`. Candidate source: frozen `93448ade` at `/tmp/text-adventure-expanded-candidate-20260908`.

All calls pass through `ReviewEvalBudget` and the shared `/tmp/text-adventure-live-eval-20260909/budget.json` ledger. Only OpenRouter `mistralai/mistral-medium-3.1` is allowed, within the user's shared $3 ceiling. The same per-request input/output bounds, context check, provider fallback prohibition and conservative reservation rules apply as in the focused evaluations. No new temperature or retry policy is introduced beyond that shared guard. Preserve actual model IDs, usage, errors and provider charges.

The additional wrapper writes a sidecar identifying engine-authored factual fallback scenes. The existing whole-run scorer predates those scenes and would otherwise score them as prose. Its implementation remains unchanged: any run with a factual fallback is explicitly ineligible for a clean model-prose comparison, while its operational behavior, original board and raw receipts are retained. Do not replace such a run after seeing its result.

Raw per-run databases, manifests, sidecars and logs remain under `/tmp/text-adventure-live-eval-20260909/confirmation-{before,after}/`. Durable summaries, copied manifests/receipts and comparison output belong under `db/eval/adversarial-20260909/confirmation-*`.

Why this confirmation is needed: the generic prompt corpus deliberately excludes `talk`, so it cannot confirm NPC agency. Its move cases exercise some arrival context, but the checked-in 90-case baseline is summary-only and cannot supply a truthful four-case subset baseline. The separately stored focused arrival/NPC experiments establish mechanism-specific before/after measurements; this unchanged sequential script checks integration through the real Turn loop without buying unrelated whole benches.

Before starting the after arm, the frozen candidate advanced to `57f8738f9e915f63ede7db7c5801d4564eb6dd50`: recorded crossing harm is displayed separately from model prose. No model instructions or inputs changed from the focused-model candidate `93448ade`. This runtime-source distinction is retained in the confirmation manifest.
