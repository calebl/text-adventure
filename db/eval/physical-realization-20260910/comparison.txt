------------------------------------------------------------------------------
REALIZATION BENCH: branches-to-corpus-after -> physical-realization-20260910
Exact rank test at p <= 0.05, 4 repetitions a side minimum.
------------------------------------------------------------------------------
schema request: no schema identity recorded
branches-to-corpus-after: mistralai/mistral-medium-3.1 | corpus de57509aa1da1a37 | prompt 08d08a01235d89d3 | 4 reps
schema request: v1:e29ef60c86aa8ae6
physical-realization-20260910: mistralai/mistral-medium-3.1 | corpus de57509aa1da1a37 | prompt d3cb3cb6f7d21a1c | 4 reps
Latencies are WARM-CACHE figures -- each arm's first realization is timed apart and excluded.

This is a PROMPT comparison on one model: 08d08a01235d89d3 -> d3cb3cb6f7d21a1c.

MODEL  mistralai/mistral-medium-3.1
  quest_target_not_admitted    0.333 -> 0.333  reported  NOISE (inside the 0.000 the unchanged runs already spanned)
  exit_into_a_written_room     0.000 -> 0.000  unchanged NOISE (inside the 0.023 the unchanged runs already spanned)
  exit_already_reachable       0.279 -> 0.276  BETTER    NOISE (inside the 0.050 the unchanged runs already spanned)
  exit_spelled_a_place_differently 0.220 -> 0.202  BETTER    NOISE (inside the 0.061 the unchanged runs already spanned)
  exit_named_this_room         0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  exit_over_the_allowance      0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  no_new_ground                0.433 -> 0.400  BETTER    NOISE (inside the 0.267 the unchanged runs already spanned)
  person_over_the_allowance    0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  item_over_the_allowance      0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  name_already_spoken_for      0.000 -> 0.000  unchanged NOISE (inside the 0.009 the unchanged runs already spanned)
  proposal_refused             0.020 -> 0.027  WORSE     NOISE (inside the 0.029 the unchanged runs already spanned)
  readable_without_words       0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  room_name_refused            0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  room_name_already_taken      0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  inside_declined              0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  inside_on_a_place_that_already_exists 0.824 -> 0.770  BETTER    NOISE (inside the 0.239 the unchanged runs already spanned)
  inside_where_the_world_wanted_none 0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  no_inside_where_the_world_wanted_one 0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  population_declined          0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  people_short_of_the_pick     0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  parameters_declined          0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  parameters_the_engine_narrowed 0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  race_not_named               1.000 -> 1.000  unchanged NOISE (inside the 1.000 the unchanged runs already spanned)
  size_the_records_do_not_hold 0.000 -> 0.000  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  insides_given                0.407 -> 0.420  reported  NOISE (inside the 0.119 the unchanged runs already spanned)
  insides_reaching             0.069 -> 0.091  reported  NOISE (inside the 0.120 the unchanged runs already spanned)
  rooms_laid_out               17.00 -> 17.00  reported  NOISE (inside the 12.000 the unchanged runs already spanned)
  storeys_below_ground         3.00 -> 3.00  reported  NOISE (inside the 3.000 the unchanged runs already spanned)
  hazard_on_the_ground_floor   0.200 -> 0.200  reported  NOISE (inside the 0.200 the unchanged runs already spanned)
  hazard_below_ground          0.750 -> 0.750  reported  NOISE (inside the 0.750 the unchanged runs already spanned)
  populations_given            1.000 -> 1.000  reported  NOISE (inside the 0.000 the unchanged runs already spanned)
  crowds_picked                0.647 -> 0.645  reported  NOISE (inside the 0.077 the unchanged runs already spanned)
  people_named                 1.24 -> 1.24  reported  NOISE (inside the 0.000 the unchanged runs already spanned)
  people_offered               1.24 -> 1.24  reported  NOISE (inside the 0.000 the unchanged runs already spanned)
  people_take_up               1.000 -> 1.000  reported  NOISE (inside the 0.000 the unchanged runs already spanned)
  items_named                  1.29 -> 1.41  reported  NOISE (inside the 0.310 the unchanged runs already spanned)
  exits_named                  2.26 -> 2.34  reported  NOISE (inside the 0.105 the unchanged runs already spanned)
  new_places_opened            0.45 -> 0.61  reported  NOISE (inside the 0.263 the unchanged runs already spanned)
  new_places_named             0.84 -> 0.97  reported  NOISE (inside the 0.211 the unchanged runs already spanned)
  exits_restating              0.623 -> 0.589  reported  NOISE (inside the 0.083 the unchanged runs already spanned)
  refusals                     0 -> 0  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  failures                     0 -> 0  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  omitted_fields               0 -> 0  unchanged NOISE (inside the 0.000 the unchanged runs already spanned)
  cap_hits                     1 -> 1  unchanged NOISE (inside the 3.000 the unchanged runs already spanned)
  latency_median               5.87s -> 6.54s  WORSE     NOISE (inside the 3.074 the unchanged runs already spanned)
  latency_p95                  11.68s -> 12.30s  WORSE     NOISE (inside the 7.411 the unchanged runs already spanned)
  output_tokens                21446 -> 22357  reported  REAL (p=0.0286)

A REAL verdict on a CHECK is what means a prompt change worked. Read the reported counts
beside it: a fall in every rate with a fall in `exits_named`, `new_places_opened` or
`people_named` is a prompt that built emptier rooms, which is the one way to improve
these numbers without improving the game (Eval::Realization::Scorer).
Everything INCONCLUSIVE at these sample sizes means: not enough repetitions.
Confirm a REAL verdict with `rake eval:run` before believing it.
------------------------------------------------------------------------------
