NPC character instructions, action choices, narrated exchanges, effect receipts, schemas and conversation history had retained study snapshots but no standing request identity. `Eval::Dialogue` now rebuilds both passes through `InteractionAgent` and the real action gate, stores complete requests and receipts, and checks their identity against current code offline.

The corpus preserves the fictional key, door, ceasefire, entrusted-key refusal and absent-item situations. Added fixtures cover staying behind, a rejected stale gift, following across an actual engine move and renewed fighting after a truce. State checks read resulting records. The original human contradiction rubric is accepted as annotated input beside those checks; new passages have no human judgments yet and explicitly report unavailable. Voice, memory fidelity across turns, long-term character behavior and personality are not measured.

An `Eval::Dialogue` sibling keeps the existing run/score/board/compare/digest vocabulary because `Eval::Prompt`'s single-passage scorer cannot represent the character-to-state-to-narrator path. `eval:estimate` prices both passes from registry rates. The fixture evaluator referenced by the original study was not shipped; the new stager reconstructs its facts. The complete universe association includes the protagonist's Human race, which the study's cached race list omitted. This is a newly purchased baseline, not relabelled old output.

The kept set exposes a staying-behind miss: choosing `none` preserves the previous following agreement, and the NPC follows through the subsequent move. Other requested state checks pass. Results remain frozen; no answer was selected or replaced.

Pricing and usage are recomputable in `doc/evidence/ta-bench-npc-dialogue/receipts-summary.json`: estimated $0.03266928, registry-priced usage $0.03263544, partial provider-reported billing $0.02020544, conservative budget accounting $0.172346 against the authorized $2 cap. All requested cases were purchased at `Eval::Noise::MIN_RUNS` on the study's pinned `mistralai/mistral-medium-3.1`. No cases remain unbought. Unknown reservations are retained by the spending guard.

No production prompt text, schema description, enum, game-engine code, migration or UI changed. No post-update action is required. The new measurement files and kept set are registered in `Eval::MEASUREMENT_FILES`.

Validation: `bin/rails test` (including engine sweep), `bundle exec rubocop`, `bin/rails zeitwerk:check`, and `bin/brakeman --no-pager`; offline full-request replay and self-comparison also pass. Human contradiction remains unavailable in the self-comparison rather than becoming a false clean score. Evidence and command logs are under `doc/evidence/ta-bench-npc-dialogue/`.
