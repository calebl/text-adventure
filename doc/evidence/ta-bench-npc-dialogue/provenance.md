The NPC study's raw before, after, excluded instrumentation-failure batch,
protocol, contradiction annotations, comparisons and budget receipts were read
alongside the scout's named dialogue/schema rows and landed-study section.
The original fixture evaluator `npc-agency-eval.rb` is named only by hashes in
the manifests; its source is not shipped. The retained budget helper is the
streaming-v2 implementation. The standing guard derives from that helper,
retains unknown reservations, and uses this task's lower ceiling.

The stager reconstructs the preserved fictional situations from their stored
requests. It uses stable reserved IDs for reproducible action tokens and rolls.
Its complete universe race association includes the protagonist's Human race;
the original study's cached list showed only Elf and Dwarf. These are new
measurements, not relabelled old responses. The original five lines and NPC
sheet facts are preserved. Added cases cover staying, a stale offered gift,
following across an actual offline move, and renewed fighting after a truce.
Staying replays the study's fixed follow exchange as durable conversation
history; this detects history changes without claiming memory quality.

The original contradiction protocol remains the rubric. Board annotations
require a narration digest, a reason and an actual displayed excerpt for a
positive. Unannotated passages remain unavailable. No model judges prose.
The narration is judged against immediate state; follow-up move/attack state
is separate, preventing a later attack from contradicting an earlier truce.
