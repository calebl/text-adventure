Schema-only prompt edits previously moved none of the standing benchmark identities, and the classifier recorded only a corpus digest. New prompt, realization, and classifier runs now store a versioned `request_identity` beside every existing identity. The emitted JSON schema, rendered system messages, and designated user messages contribute to it. Offline digest tasks print the same identity without a key or model call.

Compatibility is additive: all kept files and legacy digest algorithms remain unchanged. Old sets print **no schema identity recorded**, never a newly inferred stale schema. Results preserve the field through save/load and summary conversion. Scores and boards print it; comparisons apply the existing prompt mismatch policy, with the classifier retaining its warning policy. Kept-set tests still read every historical set, and the measurement manifest includes the new builders.

Normalization fixes realization slot data before rendering and exercises every engine-supported population count. This preserves the literal people/slot directions and the dynamic schema variants that the legacy whole-line scrub missed. The classifier renders the lowest labelled line's real staged position, retaining seed-owned closed-set names and sizes while excluding database ids and unrelated rolls. Tests show that instructions and closed-set labels move its deterministic identity. A schema-only change to the nested exits `inside` description moves the new realization identity while leaving every legacy identity unchanged.

This measures designated request scaffolds, not every conversation branch or output behavior. Generated assistant detail replayed before exits and variable ending preludes are outside scope; ending assembly uses explicitly synthetic fixed prelude prose. The instrument cannot score an unscored behavior or manufacture schemas that historical sets never recorded. No kept set was re-bought: `ta-bench-rebaseline-stale` owns that follow-up. No gameplay prompt, schema, seed, engine implementation, or migration changed.

Estimated spend: **$0**. Actual spend: **$0**, no model calls. Evidence and reproducible commands are in `doc/evidence/ta-bench-schema-digest/`.

Validation: `bin/rails test` (including engine sweep), `bundle exec rubocop`, `bin/rails zeitwerk:check`, `bin/brakeman --no-pager`, offline digest commands for all benches and ending, and `rake eval:manifest` pass. The existing rake task-loading tests print constant-redefinition warnings; the suite finishes without failures or errors. Brakeman reports no security warnings.
